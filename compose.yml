services:
  next-dev:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1
    command: sh -c "npm ci && npm run dev"
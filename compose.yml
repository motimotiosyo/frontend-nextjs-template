services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1
    command: npm run dev

volumes:
  node_modules:
